# Review Rule

- Default to defects-first reporting.
- Order issues by severity: S0, S1, S2.
- Each finding must include file and line.
- Keep summaries brief after findings.
- When proposing fixes, require corresponding entry in `CHANGELOG.md` and `docs/CHANGE_RECORDS.md`.
