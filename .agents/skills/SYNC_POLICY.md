# Skill Sync Note

Canonical skill sources are under `.codex/skills`.
Mirror updates to `.agents/skills` after each change.
Every sync change must be recorded in `CHANGELOG.md` and `docs/CHANGE_RECORDS.md`.
